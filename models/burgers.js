const orm = require('../config/orm');

const burger = {
    name: 'burgers_db',

    listAll: async () => {

    },

    cookNew: async (burgerName) => {

    },

    devourThis: async (burgerID) => {

    }
};

module.exports = burger;
