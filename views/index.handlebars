<div class="jumbotron jumbotron-fluid">
    <div class="container">
        <h1>Eat-Da-Burger!</h1>
        <h4>Cook Yarselph a Burga! NOM NOM NOM!</h4>
    </div>
</div>

{{#each burgers}}
{{#unless devoured}}
    {{burger_name}}
{{/unless}}
{{/each}}

{{#each burgers}}
{{#if devoured}}
    {{burger_name}}
{{/if}}
{{/each}}